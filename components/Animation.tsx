import { Text } from '@/components/ui/text';

interface AnimationProps {
  streak: number
}

export default function Animation({streak}: AnimationProps) {
  
  return (
    <>
      <Text>a</Text>
    </>
  )
}